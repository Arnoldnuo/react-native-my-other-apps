export interface AppInfo {
  key: string
  name: string
  subtitle: string
  avatar: string // 图标url
  androidUrl?: string
  androidVersion?: string
  iosUrl?: string
  iosVersion?: string
  whatsNew?: string // 版本更新的内容
}
